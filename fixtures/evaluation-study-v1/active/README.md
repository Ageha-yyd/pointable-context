# Relay Cache

Relay Cache keeps bounded development-context records available for deterministic point lookup.

## Refresh behavior

The open card reports a detected revision first. It reloads content only after a trusted refresh action and preserves the card in place.

## Scope

The workspace contains only local demonstration files and never represents a live project.
