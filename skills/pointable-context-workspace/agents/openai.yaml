interface:
  display_name: "Pointable Context Workspace"
  short_description: "Bind Quiet Context Reveal to a local workspace"
  default_prompt: "Use $pointable-context-workspace to enable Quiet Mode and explicitly bind this Codex task to my local workspace."

policy:
  allow_implicit_invocation: false
